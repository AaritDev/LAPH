# Note: Tkinter is provided by the system package (e.g. `python3-tk`); it may not be installable via pip
# The list below contains all Python packages required by the application
click
requests
psutil
Pillow
pyautogui
rich
ttkbootstrap
toml
pytest
